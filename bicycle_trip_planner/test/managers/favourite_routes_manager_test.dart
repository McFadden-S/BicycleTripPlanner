/*import 'package:test/test.dart';
import 'package:bicycle_trip_planner/managers/FavouriteRoutesManager.dart';

main(){
  final favouriteRoute = FavouriteRoutesManager();

  test('ensure can get necessary fields', (){
    expect(favouriteRoute.getNumberOfRoutes(), 0);
    expect(favouriteRoute.getFavouriteRoutes(), {});
  });

  test('ensure get key and get route by index return correct values when route is empty', (){
    expect(favouriteRoute.getKey(1), "");
    expect(favouriteRoute.getFavouriteRouteByIndex(1), null);
  });
}*/